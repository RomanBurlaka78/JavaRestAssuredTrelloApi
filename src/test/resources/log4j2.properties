appender.console.type = Console
appender.console.name = STDOUT
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n

rootLogger.level = info
rootLogger.appenderRef.stdout.ref = STDOUT

# ??????? ??????????? ?? ?????????
rootLogger.appenderRefs = console, elastic
rootLogger.appenderRef.console.ref = Console
rootLogger.appenderRef.elastic.ref = Elasticsearch

# ???????? ??? Elasticsearch
appender.elastic.type = Http
appender.elastic.name = Elasticsearch
appender.elastic.url = http://localhost:9200/_bulk
appender.elastic.layout.type = JsonLayout
appender.elastic.layout.compact = true
appender.elastic.layout.eventEol = true
appender.elastic.layout.properties = true
appender.elastic.headers.type = Property
appender.elastic.headers.name = Content-Type
appender.elastic.headers.value = application/json